import './App.css';
import './style.css'
import TodoScreen from './screens/TodoScreen..jsx'

function App() {
  return (
    <TodoScreen />
  );
}

export default App;



//INSTRUCTIONS.
//prioritize the task by heart liking.
//mark the task as done so it will go into the history section and remove from the todo-list.
//user can just directly delete the task as well so it will permenantly removed.
//user can edit the task as well to do this, it will appear on the edit bar on top where we write our task and user can edit it and save again, the previous should deleted on its own.
